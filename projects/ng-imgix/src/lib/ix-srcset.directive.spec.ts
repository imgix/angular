import { IxSrcsetDirective } from './ix-srcset.directive';

describe('IxSrcsetDirective', () => {
  it('should create an instance', () => {
    const directive = new IxSrcsetDirective();
    expect(directive).toBeTruthy();
  });
});
